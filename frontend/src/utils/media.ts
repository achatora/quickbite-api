import type { MenuItem, SessionUser } from "../types";

export function getMenuItemImageUrl(item: MenuItem) {
  void item;
  return null;
}

export function getUserAvatarUrl(user: SessionUser | null) {
  void user;
  return null;
}
